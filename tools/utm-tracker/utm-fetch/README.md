
# 数据回收方案矩阵 (UTM Fetch Matrix)

> **职能**：实现从 **UTM 链接发布** 到 **数据自动回流 report/** 的闭环。

---

## 📊 UTM 转化追踪的底层实现方案

> 要实现从 **UTM 链接发布** 到 **数据自动回流 report/** 的闭环，不能靠营员口头汇报，必须依赖一套底层的**数据审计逻辑**。

目前我们作为一个开源仓库，主要通过以下三种方式抓取这些标签数据：

### [方案 A：利用 GitHub 官方自带流量分析 (Native)](./github-native/)

GitHub 仓库的 `Insights -> Traffic` 页面会自动记录 **Referring sites**。

* **适用**：零成本，小白适用。
* **原理**：当营员在知乎发布了 `github.com/repo?utm_source=zhihu`，GitHub 会识别来源域名为 `zhihu.com`。
* **局限**：GitHub 网页端只展示前 14 天的数据，且无法直接按 `utm_campaign` 细分。
* **操作**：营员需每周手动截图 `Traffic` 数据，填入 `report/`。

### [方案 B：利用中间跳转页 (Custom Proxy)](./custom-proxy/)（Vercel/Cloudflare Workers 自建，推荐）

这是最能体现“方法论”的方案。我们不直接发 GitHub 链接，而是发一个我们自建的短链接或跳转页。

* **适用**：需要精确 Campaign 审计的项目。
* **原理**：自建短链接跳转，在跳转瞬间记录数据至数据库。
1. **链路**：用户点击 `link.upstream.com/go?utm_source=v2ex` -> 跳转到 GitHub。
2. **实现**：在跳转的瞬间，用一行代码将 UTM 参数记录到数据库（如 Supabase 或 Simple Analytics）。
3. **回流**：编写一个简单的脚本 `fetch_stats.py`，定期从数据库调取数据，自动生成 Markdown 表格并写入 `report/`。

### [方案 C：第三方专业分析工具 (Professional)](./third-party-analytics/)（Umami / Plausible）

如果你有项目官网（Homepage），可以使用 Umami 这种开源、隐私保护的统计工具。

* **适用**：有项目官网的情况。
* **工具**：Umami 或 Plausible。
* **实现**：在 `publishing-standards.md` 中定义的 UTM 格式是标准化的，这些工具能直接识别并在后台生成“渠道转化率”报表。
* **回流**：营员只需在周报中粘贴 Umami 的分享看板链接。

---

## 📈 数据回收 SOP (How to Close the Loop)

生成链接并发布后，数据回收按以下层级进行：

1. **初级（手动）**：发布 48 小时后，观察对应平台的点赞/评论数，填入 `distribution-log.md`。
2. **进阶（看板）**：访问项目官方统计后台（如 Umami），导出对应 `utm_source` 的点击量。
3. **闭环（周报）**：将统计到的“点击 -> Star/Fork”转化率写进 `report/` 目录下的周报文件中，作为项目决策依据。


---
*Upstream Logbook | Powered by Upstream Labs*


